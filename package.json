{
  "name": "@toolbuilder/make-factory",
  "version": "0.1.1",
  "description": "Generate a factory class from a class constructor function. Adds static methods from all super classes",
  "main": "index.js",
  "module": "src/factory.js",
  "scripts": {
    "build": "npm-run-all --serial lint test",
    "lint": "eslint src test",
    "prepublishOnly": "npm run build",
    "test": "tape -r esm test/*_test.js | tap-nirvana"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/toolbuilder/make-factory"
  },
  "files": [
    "src",
    "index.js"
  ],
  "keywords": [
    "javascript",
    "factory",
    "new"
  ],
  "author": "Kevin Hudson",
  "license": "MIT",
  "dependencies": {
    "esm": "^3.2.25"
  },
  "devDependencies": {
    "@toolbuilder/eslint-config": "^0.1.0-rc1",
    "eslint": "^6.3.0",
    "npm-run-all": "^4.1.5",
    "tap-nirvana": "^1.1.0",
    "tape": "^4.11.0"
  },
  "eslintConfig": {
    "extends": "@toolbuilder/eslint-config"
  }
}
